"""FastAPI routers.

- chat    — POST /chat (shared by web + mobile)
- mobile  — GET /weather, /advisory, /historical, /comparison (Flutter screens)
- dev     — GET /health, /dev, /fusion, POST /dev/sandbox (web Dev Suite + monitoring)
"""
